import { SwiperButton } from './button';
import { SwiperContent } from './content';
import { SwiperItem } from './item';
import { SwiperRoot } from './root';

const Root = SwiperRoot;
const Content = SwiperContent;
const Item = SwiperItem;
const Button = SwiperButton;

export { Button, Content, Item, Root };
